<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.data2.project.system.role.mapper.RoleMapper">
    <!--根据用户ID查询角色-->
    <select id="selectRolesByUserId" resultType="com.data2.project.system.role.entity.Role">
        select r.role_id, r.role_name, r.role_key from sys_role r
        left join sys_user_role ur on r.role_id = ur.role_id
        where ur.user_id=#{userId}
    </select>
</mapper>